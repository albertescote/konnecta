<?xml version="1.0" encoding="utf-8"?>
<!--
    Excludes all app data from cloud backups and device-to-device transfers.
    Auth tokens and session data must never leave the device via backup.
    Applies on Android 12+ (API 31+). For older versions, allowBackup="false" in the manifest covers it.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
